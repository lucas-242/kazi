name: kazi
description: An app to keep track of your personal or work services.
publish_to: 'none'
version: 1.2.0+15

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  cloud_firestore: ^6.0.1
  dropdown_search: ^6.0.2
  equatable: ^2.0.7
  firebase_analytics: ^12.4.5
  firebase_auth: ^6.5.6
  firebase_core: ^4.12.1
  firebase_crashlytics: ^5.2.6
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.1
  flutter_localizations:
    sdk: flutter
  flutter_masked_text2: ^0.9.1
  flutter_native_splash: ^2.4.6
  flutter_svg: ^2.2.1
  get_it: ^8.2.0
  google_mobile_ads: ^6.0.0
  google_sign_in: ^7.2.0
  kazi_core:
    path: ../kazi_core
  # Direct dependency required so riverpod_generator's builder auto-applies to
  # this package (widgets/runtime come transitively via the kazi_core barrel).
  riverpod_annotation: ^3.0.3

dev_dependencies:
  build_runner: ^2.7.1
  fake_cloud_firestore: ^4.0.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  riverpod_generator: ^3.0.3

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/svgs/
    - .env.prod
    - .env.staging
    - .env.prod_test
