# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
**/doc/api/
.dart_tool/
build/

# Kazi
.env.*
google-services.json

# Coverage reports, produced by `melos run coverage` — both the per-package
# output and the aggregated one at the root.
#
# Note: packages/kazi/coverage/lcov.info was committed before this rule and
# stays tracked until it is removed with `git rm --cached`.
coverage/